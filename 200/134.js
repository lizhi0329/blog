/**
 * @param {number[]} gas
 * @param {number[]} cost
 * @return {number}
 */
var canCompleteCircuit = function(gas, cost) {
  let end = 0
  const n = gas.length
  let step = 0

  for(let i = 0; i < n; i++) {
    if (step ===  n) {
      
    }
  }
  // process()
};


const gas = [1,2,3,4,5]
const cost = [3,4,5,1,2]

canCompleteCircuit(gas, cost)